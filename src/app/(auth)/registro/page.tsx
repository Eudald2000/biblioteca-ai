import { RegisterForm } from '@/components/features/auth/RegisterForm'

export const metadata = { title: 'Crear cuenta — Biblioteca Virtual' }

export default function RegistroPage() {
  return <RegisterForm />
}
